# Task Folder - Movie Analysis Project

## Description
This folder contains the files related to the **movie analysis project task** uploaded on **April 1, 2026**. 

## What We Have Done Today
- Prepared the `task` folder containing only the relevant files.  
- Files included:
  - `sqltask.py` – Python script for SQL operations.  
  - `movie_reviews.db` – SQLite database for movie reviews.  
  - `WhatsApp Image 2026-04-01 at 1.54.43 PM.jpeg` – Reference image for the task.  
  - `.vscode/settings.json` – VS Code workspace settings.  
- Created a new GitHub repository: `movie-analysis-task`.  
- Uploaded only the `task` folder to the `main` branch of the repository.  
- Set up Git and connected the local folder with the remote repository.  

## Notes
- This is a clean version; the previous wrong directory upload has been removed.  
- The `main` branch is now the default branch for future updates.  
- All future changes to the task can be committed and pushed directly to `main`.

---
